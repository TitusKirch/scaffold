# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json

# Opens and updates the rolling draft promotion PR. The body picks its target by
# asking whether a `stage` branch exists, so this file is identical whether the
# repo runs `dev → main` or `dev → stage → main`.
name: Promotion PR

on:
  push:
    branches:
      - dev
      - stage

permissions:
  contents: read
  pull-requests: write

jobs:
  promotion-pr:
    name: Promotion PR
    uses: kirchDev/workflows/.github/workflows/_promotion-pr.yml@f47f598f549a3c97924fdfd00f15371ea557e1aa # v0.1.1
